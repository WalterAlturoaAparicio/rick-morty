type Character {
  id: Int!
  name: String!
  status: String
  species: String
  gender: String
  origin: String
  image: String
}

input CharacterFilter {
  name: String
  status: String
  species: String
  gender: String
  origin: String
}

type Query {
  characters(filter: CharacterFilter): [Character]
}
